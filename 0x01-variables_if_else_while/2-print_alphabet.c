#include <stdio.h>

/**
  *main - Entry point
  *Description: Print alphabets lowercase
  *Return: Always 0
  */
int main(void)
{
	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
return (0);
}
